#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=scripts/_retention_lib.sh
source "$SCRIPT_DIR/_retention_lib.sh"
require_db

"$SCRIPT_DIR/retention-verify"
echo "DB: $RETENTION_DB"
