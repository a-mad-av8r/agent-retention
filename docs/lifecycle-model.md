# Lifecycle Model

```text
hot -> warm -> cold -> archived
```

Hot memory is used in session boot. Warm memory is searchable. Cold memory is
rarely used but still available. Archived memory leaves the active path but
keeps enough structure to be found later.
