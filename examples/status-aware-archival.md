# Status-Aware Archival

Retention should consider status, not only age.

Examples:

- Keep open handoffs hot until they close.
- Keep active sprint decisions warm until the sprint completes.
- Archive old chat messages aggressively once durable decisions and lessons have
  been extracted.

The public demo keeps the logic simple so the lifecycle is easy to inspect.
